import { Component } from '@angular/core';

// Placeholder: el catalogo completo (filtro, busqueda, paginacion) se
// implementa en el siguiente commit. Este stub solo habilita la ruta
// para poder probar el flujo de login/registro de punta a punta.
@Component({
  selector: 'app-catalog',
  standalone: true,
  template: `<p style="padding: 24px">Catálogo en construcción.</p>`,
})
export class CatalogComponent {}
